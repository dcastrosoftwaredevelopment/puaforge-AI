# Server
PORT=3001

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=puaforge
DB_PASSWORD=puaforge
DB_NAME=puaforge

# Auth
JWT_SECRET=change-me-in-production

# Google OAuth (get from console.cloud.google.com)
# Used by both server (token verification) and frontend (OAuth button)
GOOGLE_CLIENT_ID=
